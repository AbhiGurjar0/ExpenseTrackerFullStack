<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaderboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div
        class="min-h-screen bg-gradient-to-br from-gray-900 via-purple-900 to-black flex flex-col items-center py-10 px-4">

        <div class="text-center mb-10">
            <h1 class="text-4xl md:text-5xl font-extrabold text-white tracking-tight">🏆 Leaderboard</h1>
            <p class="text-gray-400 mt-2">Track the top performers in real-time</p>
        </div>


        <div
            class="w-full max-w-4xl bg-white/5 backdrop-blur-sm rounded-xl overflow-hidden border border-white/10 shadow-lg">

            <div
                class="grid grid-cols-3 md:grid-cols-4 gap-4 px-6 py-4 bg-gradient-to-r from-purple-800 to-purple-600 text-white font-semibold text-lg">
                <span class="col-span-1">Rank</span>
                <span class="col-span-1">User</span>
                <span class="col-span-1 text-center">Expense</span>
            </div>


            <div class="divide-y divide-white/10">

                <%users.forEach((user,index)=>{%>
                    <div
                        class="grid grid-cols-3 md:grid-cols-4 items-center px-6 py-4 hover:bg-white/5 transition duration-150 group">
                        <span class="font-bold text-yellow-400 flex items-center">
                            #<%= index + 1 %>
                                <% if(index==0){ %>
                                    <span class="ml-2 text-xs bg-yellow-400/20 px-2 py-1 rounded-full">TOP</span>
                                    <% } %>


                        </span>
                        <div class="flex items-center gap-3">

                            <div>
                                <span class="text-white font-medium block">
                                    <%=user.userName%>
                                </span>
                                <span class="text-xs text-gray-400 md:hidden">Level 45</span>
                            </div>
                        </div>

                        <span class="text-yellow-300 font-semibold text-center">
                            <%=user.totalExpense%>
                        </span>
                    </div>

                    <%})%>






            </div>
        </div>


        <a href="/">
            <div class="mt-10 text-center">
                <button
                    class="px-6 py-3 bg-gradient-to-r from-purple-600 to-purple-500 hover:from-purple-500 hover:to-purple-400 text-white font-bold rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
                    Go to Home 🚀
                </button>
            </div>
        </a>
    </div>
</body>

</html>